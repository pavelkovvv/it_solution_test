from django.apps import AppConfig


class RunningLineConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'running_line'
    verbose_name = 'Скачивание файла с бегущей строкой'
